<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>LAB3</title>
</head>
<body>
<%- include('header') %>
<div class="container">
    <div class="row">
        <div class="col-12">
            <% if (isEdit) { %>
                <h1 class="text-center">Uredi korisnika</h1>
            <% } else { %>
                <h1 class="text-center">Novi korisnik</h1>
            <% } %>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <form id="addUserForm" action="/korisnici" method="post">
                <input type="hidden" name="isEdit" value="<%= isEdit %>">
                <div class="mb-3">
                    <label for="oib" class="form-label">OIB</label>
                    <% if (isEdit) { %>
                        <input type="text" class="form-control" id="oib" name="oib" aria-describedby="oibHelp"
                               value="<%= user.oib %>" readonly>
                    <% } else { %>
                        <input type="text" class="form-control" id="oib" name="oib" aria-describedby="oibHelp"
                               value="<%= user.oib %>">
                    <% } %>
                    <div id="oibHelp" class="form-text">Must be exactly 11 digits.</div>
                    <% if (error.oib) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error.oib %>
                        </div>
                    <% } %>
                </div>
                <div class="mb-3">
                    <label for="name" class="form-label">Ime</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= user.name %>">
                    <!-- if errorName show error label with error text -->
                    <% if (error.name) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error.name %>
                        </div>
                    <% } %>
                </div>
                <div class="mb-3">
                    <label for="surname" class="form-label">Prezime</label>
                    <input type="text" class="form-control" id="surname" name="surname" value="<%= user.surname %>">
                    <% if (error.surname) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error.surname %>
                        </div>
                    <% } %>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>">
                    <% if (error.email) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error.email %>
                        </div>
                    <% } %>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Lozinka</label>
                    <input type="password" class="form-control" id="password" name="password">
                    <% if (error.password) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error.password %>
                        </div>
                    <% } %>
                </div>
                <div class="mb-3">
                    <label for="job" class="form-label">Opis zaduženja</label>
                    <input type="text" class="form-control" id="job" name="job" value="<%= user.job %>">
                    <% if (error.job) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error.job %>
                        </div>
                    <% } %>
                </div>
                <button type="submit" class="btn btn-success">Spremi</button>
            </form>
        </div>
    </div>
</div>
</body>
</html>